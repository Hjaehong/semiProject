<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.semiTravelRecommend.model.dao.CommentMapper">

    <resultMap type="com.team.semiTravelRecommend.model.dto.comment.CommentDTO" id="commentResultMap">
        <id property="comNo" column="COM_NO"/>
        <result property="userNo" column="USER_NO"/>
        <result property="recordNo" column="RECORD_NO"/>
        <result property="comContain" column="COM_CONTAIN"/>
    </resultMap>

    <insert id="insertComment">
            INSERT INTO OH_COMMENT A (
                A.COM_NO ,
                A.USER_NO ,
                A.RECORD_NO ,
                A.COM_CONTAIN
            )
            VALUES (
                SEQ_COM_NO.NEXTVAL ,
                #{ userNo } ,
                #{ recordNo } ,
                #{ comContain }
                )

    </insert>

    <select id="viewAllComment" resultMap="commentResultMap">
        select
            *
        from OH_COMMENT A
        join OH_USER B on A.USER_NO = B.USER_NO
        where A.RECORD_NO = #{ recordNo }
    </select>

<!--    <update id="updateComment" parameterType="">-->
<!--        update COMMENT-->
<!--        set COM_CONTAIN = #{}-->
<!--    </update>-->
</mapper>